# DisastropheBotV2
Discord bot that gives users upon having a special status

In settings:

# Prefix = You can put any prefix you want, but this bot uses /

setup = {custom_status} {role} 

setup_counter (send a embed, essential for counter members) 

embed_edit (like the name says, you can edit the embed as you want).
